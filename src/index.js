import React from 'react';
import ReactDOM from 'react-dom';
import SidemenuSliders from './SidemenuSliders';

ReactDOM.render(<SidemenuSliders />, document.getElementById('root'));

if (module.hot) { module.hot.accept() }